apiVersion: helm.fluxcd.io/v1
kind: HelmRelease
metadata:
  name: oem-apioem-api-bnbootnode
  namespace: oem-api-subs
  annotations:
    flux.weave.works/automated: "false"
spec:
  releaseName: oem-apioem-api-bnbootnode
  chart:
    git: https://github.com/inteli-poc/bevel.git
    ref: api-sandbox
    path: platforms/substrate/charts/substrate-node

  values:
    image:
      repository: ghcr.io/digicatapult/dscp-node
      tag: v3.6.0
      pullPolicy: IfNotPresent
    nameOverride: oem-api-bn
    fullnameOverride: oem-api-bn

    serviceAccount:
      create: false
      name: vault-auth

    podSecurityContext:
      runAsUser: 1000
      runAsGroup: 1000
      fsGroup: 1000
    ingress:
      enabled: false

    node:
      name: oem-api-bn
      chain: inteli-gcp
      command: ./dscp-node      
      dataVolumeSize: 10Gi
      replicas: 1
      role: full
      customChainspecUrl: true
      customChainspecPath: "/data/chainspec.json"
      collator:
        isParachain: false
                
      enableStartupProbe: false
      enableReadinessProbe: false
      flags:
        - "--rpc-external"
        - "--ws-external"
        - "--rpc-methods=Unsafe"
        - "--rpc-cors=all"
        - "--unsafe-ws-external"
        - "--unsafe-rpc-external"
      keys:
        - type: "gran"
          scheme: "ed25519"
          seed: "grandpa_seed"
        - type: "aura"
          scheme: "sr25519"
          seed: "aura_seed"
      persistGeneratedNodeKey: false
      
      resources: {}
      serviceMonitor:
        enabled: false
        
      perNodeServices:
        createClusterIPService: true
        createP2pNodePortService: true
        p2pServiceType: ClusterIP
        setPublicAddressToExternalIp:
          enabled: false
          ipRetrievalServiceUrl: https://ifconfig.io
      podManagementPolicy: Parallel

      ports:
        p2p: 30333
        ws: 9944
        rpc: 9933

      tracing:
        enabled: false
      substrateApiSidecar:
        enabled: false

    proxy:
      provider: ambassador
      external_url: oem-api-bn.uat.intelipoc.com
      p2p: 15057
      certSecret: oem-api-ambassador-certs

    storageClass: gcpstorageclass

    vault:
      address: http://vault.internal.uat.intelipoc.com:20004      
      secretPrefix: secretsv2/data/oem-api-subs
      authPath: substrateoem-api
      appRole: vault-role
      image: ghcr.io/hyperledger/alpine-utils:1.0 
